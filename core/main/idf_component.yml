## IDF Component Manager Manifest
##
## Pulls esp_littlefs into managed_components/ at first build so we can bake
## NES ROMs into the LittleFS partition image without vendoring the whole
## driver locally.
dependencies:
  joltwallet/littlefs: "^1.10.0"
  idf:
    version: ">=5.0"

## IDF Component Manager Manifest
##
## ROMs live in a raw 'roms' partition built at compile time and accessed
## at runtime via esp_partition_mmap (flash XIP). No filesystem component
## is needed.
dependencies:
  idf:
    version: ">=5.0"
